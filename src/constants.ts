export const FALLBACK_BOARD_WIDTH = 20;
export const FALLBACK_BOARD_HEIGHT = 20;
export const FALLBACK_INTERVAL_MS = 60; // snake speed
export const CRASHFLASH_INTERVAL_MS = 460;
export const FALLBACK_BOARD_SIZE = 20;
